[build]
  publish = "."
  functions = "netlify/functions"

# Credenziali API GME — aggiorna GME_PASSWORD dopo aver cambiato la password
[build.environment]
  GME_USERNAME = "madesrls3274"
  GME_PASSWORD = "ukEXkZQI.a8+Yg%"

[functions]
  node_bundler = "esbuild"

# Headers per sicurezza e performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache per assets statici
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache per immagini
[[headers]]
  for = "/assets/img/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Redirect www a non-www
[[redirects]]
  from = "https://www.semplicom.com/*"
  to = "https://semplicom.com/:splat"
  status = 301
  force = true

# 404 fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
